import { rollupStats } from './plugin';

export type {
  RollupStatsOptions,
  RollupStatsOptionsOrOutputOptions,
} from './plugin';

export type {
  OutputAsset,
  OutputBundle,
  OutputChunk,
  RenderedModule,
  SourceMap,
} from './types';

export default rollupStats;
