{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    // The root config sets `removeComments: true`; keep them here so the JSDoc on
    // the public `Plugin`/`OutputOptions` types survives into the published .d.ts.
    "removeComments": false
  },
  "include": ["src/**/*"],
  "exclude": ["**/*.test.ts"]
}
