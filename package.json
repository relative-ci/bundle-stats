{
  "name": "@relative-ci/ui",
  "version": "0.0.1",
  "description": "Relative CI shared UI",
  "main": "lib/index.js",
  "scripts": {
    "build-css": "find src -name \\*.css -exec cp {} lib \\; ",
    "build": "rm -fr ./lib && mkdir lib && npm run build-css",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
